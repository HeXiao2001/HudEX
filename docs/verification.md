# HudEX v1 — 验证记录

所有测试都在当前机器上实测：macOS 26.6.2 (25G113 / BuildVersion 25G83)、
Xcode 26.6、Apple Swift 6.3.3、MacBook 内建 Retina 显示器 1470×956 pt @2x、
Dock 在左侧、tilesize 34（保留带 54 pt）、有菜单栏（33 pt）。

## 1. 构建与单元测试

```
swift build --product HudEX          # 成功，无 error / warning（HudEXApp + HudEXCore）
swift test                           # 97 项测试通过
./script/build_and_run.sh            # 组装 dist/HudEX.app + ad-hoc 签名 + 启动
```

测试分布：解析 12、Markdown 渲染与文件 10（含原子替换、编码回退、空文件）、
布局引擎 25（含三种模式与叠放）、Dock 几何估算 10、设置段与双向同步 9、
调色板与项目元数据 7、本地化 4、颜色与短名 15、模板 1。

## 2. Dock 三个方向（窗口列表实测）

用 `CGWindowListCopyWindowInfo` 读取 HudEX 自己窗口的真实坐标（AppKit 坐标）：

| Dock 位置 | 标签面板实测矩形 | 说明 |
|---|---|---|
| 左侧 | `x 0, y 8, w 54, h 85` | 贴左边缘，宽 54 = Dock 厚度，在 Dock 下方 |
| 下方 | `x 8, y 0, w 85, h 54` | 贴下边缘，高 54 = Dock 厚度，从左侧向右排 |
| 右侧 | `x 1416, y 8, w 54, h 85` | 贴右边缘（1470-54） |
| 再次左侧 | `x 0, y 8, w 54, h 85` | 换回后立即恢复 |

切换 Dock 方向时没有重启 HudEX：位置变化由系统通知驱动，实测切换后 1–6 秒内更新。

## 3. 文件与可靠性

| 场景 | 期望 | 实测 |
|---|---|---|
| 追加第 4 个项目 | 面板从 54×85 变 54×115 | ✅ |
| 写临时文件再 mv 覆盖（OneDrive/Obsidian 习惯） | 内容更新 | ✅（54×55，2 个项目） |
| 文件被清空（写入瞬间） | 保留上一次内容 | ✅ 面板保持 54×55，3 秒后复查 |
| 文件持续为空 | 确实清空则隐藏标签 | ✅ |
| 损坏 Markdown（`<<<>>> not markdown`） | 不崩溃、不清屏 | ✅ |
| 恢复内容 | 标签回来 | ✅（54×85） |
| 缺少/无法解析日期 | 保留原文并给 warning | ✅（单元测试） |

## 4. 交互

| 场景 | 实测 |
|---|---|
| 悬停标签 → 简介窗口 | ✅ 出现在屏幕内侧，底部 Dock 时出现在标签上方 |
| 鼠标移入简介窗口 | ✅ 保持显示（0.22 s 宽限期） |
| 鼠标离开 | ✅ 简介窗口隐藏 |
| 悬停不抢焦点 | ✅ `HudEX isActive=false`，前台应用不变 |
| 菜单栏图标 | ✅ `NSStatusBarWindow` 存在，菜单项在打开时刷新状态 |
| 关闭菜单栏图标后恢复 | ✅ 再次打开 HudEX.app 直接弹出设置窗口（`applicationShouldHandleReopen`） |
| 设置窗口 | ✅ 540×724，原生控件 |
| 手动标签尺寸（宽 40 / 高 32） | ✅ 面板变 `w 40, h 106`（3×32 + 2×5） |
| 每个位置最多 1 个标签（3 个项目） | ✅ 主位置 `y 8`、溢出位置 `y 890`（不覆盖菜单栏），第 3 个不显示 |

> 说明：合成鼠标事件（`CGEvent.post`）在当前沙箱环境下不会被投递，
> 因此“点击按钮”这类交互由用户手动验证；其余结论均来自客观测量。

## 5. 空闲性能（Release 级行为，5 分钟）

测量方式：外部进程用 `proc_pid_rusage(RUSAGE_INFO_V4)` 每 30 秒采样一次。

两组独立测量：

**A-1. 长时间操作后的内存（1.0 前复测）**

| 场景 | 物理内存 |
|---|---|
| 全新启动、只显示标签 | 15.7 MB |
| 3 轮指针扫过（约 12 张卡片弹出又收回） | 21.2 MB（之后不再增长） |
| 设置窗口停在「外观」页 + 卡片停留 + **600 次**设置变更（16 ms 一次，等同拖动滑块） | 21.0 MB |
| 关闭卡片/窗口后 | 19.6 MB |

结论：没有泄漏——只有渲染过卡片之后约 5 MB 的字体/文本布局缓存常驻，之后完全平坦。
（用户报告的 88 MB 出现在同时用过系统「打开文件」面板之后：那个面板会在**本进程内**留下
浏览/缩略图缓存，实测单独一个空面板就 +7 MB，浏览大目录更多；它的两个辅助进程
`Open and Save Panel Service` / `QuickLookUIService` 是 macOS 自己的服务，不是 HudEX 的。
因此设置页现在支持直接粘贴路径和拖放文件，把面板变成可选路径。）

**A0. 视觉功能全部落地后复测（90 秒，全新启动、只显示标签）**

| 指标 | 结果 |
|---|---|
| 空闲 CPU | 90 秒内累计 CPU 时间 **0.000 s** |
| 常驻内存 | **15.8 MB** 稳定 |
| 唤醒 | 每 30 秒 4 次 → **≈0.13 次/秒** |

（叠放、拟物纸张、贝塞尔连接线、hover 抬起这些视觉效果没有增加静止开销：
hover 只在指针跨过标签时才触发一次重绘。）

**A. 全新启动、只显示标签（90 秒）**

| 指标 | 结果 |
|---|---|
| 空闲 CPU | 采样窗口内累计 CPU 时间 **0.000 s**，平均 **0.0001–0.0002%** |
| 常驻内存 | **14.1 MB** 稳定（峰值 14.1 MB） |
| 唤醒 | 每 30 秒 5 / 7 / 10 次 → **≈0.1–0.3 次/秒** |

**B. 打开过设置/完整窗口之后（5 分钟）**

| 指标 | 结果 |
|---|---|
| 空闲 CPU | 300 秒累计 **0.000 s**，平均 **0.007%** |
| 常驻内存 | 稳定 **67.3 MB**（不再增长） |
| 网络 | **0**：二进制不链接任何网络 API（`nm -u` 中 URLSession/CFNetwork/socket 符号数 = 0），进程无打开的 socket |
| 磁盘 | 空闲时不读写：没有轮询定时器，文件只在 FSEvents 事件后且签名变化时读取 |
| 唤醒 | 启动初始化后 **≈0.5 次/秒** |

内存差异说明：67 MB 是访问过设置窗口（一个较大的 SwiftUI 表单）后的稳态；
刚启动、只显示标签时只有 14 MB。这是 AppKit + 每个窗口一个 `NSHostingView`
的 SwiftUI 运行时成本。如需进一步压低，可以把标签面板改成 CoreGraphics 自绘
（代价是失去 SwiftUI 布局），当前数值已在目标范围内。

## 6. 仍未覆盖 / 已知限制

* 全屏 App 与多 Space：使用 `.canJoinAllSpaces + .canJoinAllApplications +
  .fullScreenAuxiliary + .stationary`，在普通窗口与全屏 Space 下可见；
  没有逐 Space 自动化验证（需要人工切换）。
* 多显示器：只在 Dock 所在的那块屏幕显示，架构上不阻止扩展。
* Dock 长度仍为估算（macOS 26 的 Dock 窗口覆盖全屏，公开 API 无法量长度）。
